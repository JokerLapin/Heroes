{
"name": "boardgame-web",
"private": true,
"engines": { "node": ">=18" },
"scripts": {
"postinstall": "npm --prefix client ci --no-audit --no-fund || true && npm --prefix server ci --no-audit --no-fund || true",
"build": "npm --prefix client run build && rm -rf server/public && mkdir -p server/public && cp -R client/dist/* server/public/",
"start": "node server/index.js"
}
}
