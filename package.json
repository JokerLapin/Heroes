{
  "name": "heroes",
  "private": true,
  "version": "0.0.1",
  "engines": { "node": ">=20" },
  "scripts": {
    "postinstall": "echo \"(no-op: installs happen in build)\"",
    "build": "npm --prefix client install --no-audit --no-fund && npm --prefix server install --no-audit --no-fund && npm --prefix client run build && rm -rf server/public && mkdir -p server/public && cp -R client/dist/* server/public/",
    "start": "node server/index.js"
  }
}
